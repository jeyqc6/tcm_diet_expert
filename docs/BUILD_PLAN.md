# 实施清单:按步骤对应文件与测试

**最后更新** 2026-08-31 · 关联:`ARCHITECTURE.md`(长什么样)· `planning/roadmap.md`(按什么顺序学+做)· `DECISIONS.md`(为什么这样选)· `ENGINEERING.md`(可靠性怎么做)

> **这份文档回答的问题**:roadmap.md 按"学习+制作"排了 9 个阶段,ARCHITECTURE.md §8.1/§8.2 分别给了目录树和逐文件测试映射——但没有一份文档把"这一步该写哪个文件、写完用哪个测试验证、验证过了才能进下一步"串成一条线。本文档做这件事,是喂给编码 agent 的执行清单,不是新的设计决策来源。
>
> **怎么用**(照抄 `planning/roadmap.md` 第七节的纪律):每次只把"当前步骤"这一行 + 它指向的 ARCHITECTURE.md 章节 + DECISIONS.md 对应条目喂给编码 agent,不要把整份文档倒进去;明确要求"照设计实现,不要自己发明架构";一步做完、对应测试跑过,再进下一步。
>
> **骨架期已过**(2026-08-29):`backend/`、`api/`、`tests/`、`frontend/` 不再是 docstring 占位。`backend/agents/router.py` 已拆成 `routing.py` + `agent_loop.py`(另有 `dispatch.py`)。阶段 1–6 主链路有真代码和测试；**阶段 7 是 Partial，不是 Done**。`tests/integration/test_fallbacks.py` 已取消 skip。下面清单里的历史完成判据保留作记录，以 §0 快照和各阶段「剩余」为准。

---



## 0. 现状快照(诚实版,2026-08-29,和 ARCHITECTURE.md §9 保持一致)

**阶段标记**：1–6 主交付 **Partial**(核心做了，表内仍有缺口)；**阶段 7 Partial**；**阶段 8 Partial**（双档 eval + 切片 + 阈值已跑，B0 M1 仍 53.3%；向量官方 M1 见 `EVALUATION.md` §7.7；B2 见该行自己的结论）；**阶段 9 Partial**（英文 README + 架构图已完成；2 分钟英文录屏、Vercel+Render+Neon 云部署均未做）。不要把阶段 7/8/9 勾成 Done。

**已完成(真代码)**：RAG 摄入 + 5837 条 embedding；`conflict_rules` 灌库；MCP + 权限分层；Agent Loop；七分支路由(`routing.py`)；双 SubAgent；调和/核查；`/api/chat` SSE；profile / onboarding / sessions API；菜品拆解 + `write_memory`；会话落库 + Tier 归档算法；guardrails；Langfuse；45s/90s 超时接线；docker compose；CI lint+pytest。

**明确未完成**：独立 3 日菜谱组装管线；双侧失败 naive RAG（当前是 guardrail）；空检索静态兜底表（现有证据修复可保留带明确标注的通用知识，但不提供静态事实表）；整条 `/api/chat` 的 M3/M5（阶段 8 只跑了单次 LLM keyword）；M6/M10–M14。阶段 7 仍是 Partial。阶段 8 双档数字见 `EVALUATION.md` §7，**不要把 M1 53.3% 写成 Launch 70%**。

**2026-08-31 行为修订**：历史表格中的旧「依据不足时重跑 SubAgent」描述以
`ARCHITECTURE.md` §5.5 和 `DECISIONS.md` D15 的现行规则为准：现在只做一次
无工具证据修复，不重跑检索或 SubAgent。

历史细节行(2026-08-26–28 当时写下的完成记录)仍保留在下表，**路径以 `routing.py` / `agent_loop.py` 为准**，不再写 `router.py`。


| 已完成                                                                                                                                            | 未完成(按优先级)                                                                                           |
| ---------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| RAG 摄入管线(`ingest.py`,含 token 感知二次切分)；`dish_ingredient_map` 217 条(2026-08-28 达到 100-200 目标,见阶段 7)                                         | —                                                                                                   |
| `knowledge_chunks`/`recipes` 建表并在真实 Postgres 跑通                                                                                                | 会话归档已接；Level 1 已在 `run_agent_loop` 按 FIFO 丢最旧 chunk（阶段 7 仍 Partial） |
| git 仓库已初始化并推送(`https://github.com/jeyqc6/tcm_diet_expert.git`)                                                                                 | 整条 `/api/chat` 的 M3/M5；双侧失败无 naive RAG |
| BGE-M3 → pgvector embedding 全量跑通(5837 条)                                                                                                       | 阶段 8 单次 LLM 双档已跑（§7）；B0 M1 仍 53.3%。向量官方 M1 见 `EVALUATION.md` §7.7（labeled Recall@5 **76.7% Launch**；节气表 all_hit 53.3%） |
| `user_profile`/`diet_log`/`conversation_sessions`/`messages`/`conflict_rules`/`user_dish_aliases` 建表                                           | `naive_rag.py`/`run_baselines.py` 里内联的 LLM 调用未迁移到 adapter 层(技术债)                                    |
| `conflict_rules.jsonl` 40 条(18 verified/22 needs_source),**已真实灌进** `conflict_rules` **表**(`db/load_conflict_rules.py`,幂等/全量同步验证过)              | —                                                                                                   |
| **阶段 3 eval 冻结**(2026-08-26):`dataset.jsonl` 40 条 + `smoke.jsonl` 15 条;B0 recall@5=53.3%,见 `docs/EVALUATION.md`                                | —                                                                                                   |
| BM25 baseline + `evals/run_baselines.py`                                                                                                       | —                                                                                                   |
| MCP server 骨架 + 权限分层(`DietExpertMcpServer`,11 条单测)                                                                                             | —                                                                                                   |
| `backend/mcp_server/tools/{retrieve_tcm,retrieve_nutrition,_retrieval_common}.py`(混合检索,真实数据验证过)                                                | —                                                                                                   |
| `backend/agents/citation.py`(溯源确定性部分:引用格式/id存在性校验)                                                                                             | —                                                                                                   |
| `backend/llm/adapter.py`(超时/重试+jitter/熔断/双档切换,18 条单测)                                                                                          | —                                                                                                   |
| `backend/agents/routing.py` + `agent_loop.py`:分类与 Agent Loop 已接通(原 `router.py` 已拆)                             | 独立 3 日菜谱组装管线仍未做 |
| `backend/llm/providers/{base,anthropic_provider,openai_compatible}.py` 补上 tool_use 支持(为了 Agent Loop 能真的跑通,原实现完全没有这块)                           | —                                                                                                   |
| `backend/mcp_server/tools/query_diet_log.py`(相对日期解析 + by_ingredient/by_property/by_meal_type/raw 聚合,真实数据验证过)                                   | — |
| `user_profile` 新增 `city`/`timezone` 字段(D30),时区待决问题已解决:`user_profile.timezone` > `DIET_EXPERT_TZ` 环境变量 > 硬编码默认值三层兜底,真实数据验证过                     | —                                                                                                   |
| `backend/agents/{tcm_subagent,nutrition_subagent,_subagent_common}.py`(阶段4 #7,复用 `run_agent_loop`;资源限额 15 次 + 循环防护连续3轮无新增信息;领域隔离靠协议层白名单,日志可核查) | —                                                                                                   |
| `api/main.py` `/api/chat` SSE 闭环(阶段4 #11)——把 `classify_route`/`run_agent_loop`/两个 SubAgent/调和层/核查 pass 串成 §5.2 真实请求生命周期,前端(#12)/docker compose(#13)均已跑通端到端 | 见上一行"未完成"列:管线已接通,但 `user_profile`/`conflict_rules` 两处还没接进这条真实请求路径,不是"整条链路都没接线" |
| 首次引导全流程(阶段7,`ccmq_scoring.py`+`flow.py`+`/api/onboarding/*`+`/api/profile`) + `user_profile`/`conflict_rules` 接入 `/api/chat` 真实请求路径 | — |
| **一句话多意图路由(D32,ARCHITECTURE §5.1.1/5.1.2,不在原 roadmap 编号里,是真实使用中发现的六分支模型缺口)**：`backend/agents/routing.py` 新增 `segment_intents`/`classify_multi_task`(确定性连接词切分 + 保守激活条件，**中英双语**)+ **`classify_turn`(2026-08-27 补，LLM 兜底)**——取代 `api/main.py` 原来"先调 `classify_multi_task` 再调 `classify_route_async`"两段独立调用，合并成一次决策：规则能确定就零 LLM 成本，规则完全没命中、或连接词存在但没法确认时才打一次 LLM(`{"tasks":[...]}` 格式，长度可以是1)。`api/main.py` 新增 `_dispatch_branch`/`_stream_multi_task` + SSE `task`/`task_done` 事件。24 条单测(`test_multi_task_routing.py` 15 条 + `test_classify_turn.py` 9 条)+ 3 条集成测试(`test_api_multi_task.py`)全过；真实验证过纯规则多任务(记录+单领域/记录+记录回顾)、LLM 兜底单任务判定、LLM 兜底本身的真实调用(真实 Anthropic Haiku，`reason=llm_turn:log_write`) | ⏳ **真实测试发现的残留缺口(如实记录)**：无连接词、但整条组合消息被某条分支正则在不该匹配的位置"confidently"命中时(例："我想知道气虚质应该怎么调理饮食，我昨天都吃了什么给我查一下"——`_LOG_REVIEW` 正则搜到子串命中，直接采信，前半句丢失)，现在的触发条件不会跑 LLM 复核。要补上需要放弃"只在规则拿不准时才打LLM"，改成对每条消息都做一次LLM复核，这是明显更大的成本代价，留给以后再决定要不要做 |
| **新增 OTHER 第七分支 + 统一人在环追问机制(D33,ARCHITECTURE §5.1/§5.1.3,落实 D20 五处 agent 行为点第 3 条 + `PRD.md` §17 开放问题,2026-08-27)**：`backend/agents/routing.py` 新增 `RouteBranch.OTHER`(`_OTHER_GREETINGS` 整句锚定匹配确定性快速通道 + `classify_turn` 七选一 LLM 兜底);`backend/agents/clarification.py`(新文件)`PendingClarification`/`ClarificationStore`/`InMemoryClarificationStore`(仿 onboarding session store,key 用 `session_id`);`backend/agents/citation.py` 新增 `build_clarification_instruction`/`extract_clarification_question`/`[NEED_CLARIFICATION]` 标记约定,两个 SubAgent system prompt 拼入;`api/main.py` 新增 `_stream_other`、`_clarification_events_for` 共享 helper、`get_clarification_store` DI provider,`_stream_log_write`/`_stream_single_domain`/`_stream_dual_dispatch` 接入检查点,`_stream_chat_inner` 新增恢复分支(`allow_clarification` 参数贯穿保证 PRD §11"追问一次"上限),新增 SSE `clarification` 事件。单测:`test_clarification.py`(9条)+ `test_other_branch.py`(5条);集成测试直接扩充进 `tests/integration/test_api_chat_sse.py`(log_write 两轮追问、candidate_eval 两轮追问、OTHER 三类输入,共 7 条新增/改写);全量 594 passed。真实验证过(真实 Postgres + 真实 Anthropic Haiku):"这个能不能吃"→ 正确追问 →"红烧肉"→ 两侧 SubAgent 都给出真实结论(TCM 侧真的调用了 `retrieve_tcm`);"你好"→ 问候不触发 SubAgent;"红烧肉怎么做"→ 通用知识回答 + 免责声明；"今天天气怎么样"→ 礼貌拒绝引导回饮食话题 | ⏳ 真实验证时发现并修复一处提示词过泛化(如实记录,已修复不是遗留问题)：`build_clarification_instruction` 最初的措辞会让"红烧肉能不能吃"这种已点名具体菜品的问题也因为健康状况/做法等外围细节不全被反问,收窄为"只在判断对象本身不确定时才触发"后修复,细节见 `DECISIONS.md` D33。真实验证时还发现 candidate_eval/full_recommend 追问成功重新分发后要经过调和层,本地开发环境若未配 prod 档凭据会在这一步鉴权失败(D19 原"调和层永远 `force_prod_tier=True`"这条例外导致)——已连带修复,不是留给以后的遗留问题:D19 这条例外已于同日(2026-08-27)撤销,调和层现在跟随 `MODEL_TIER`,见 D19 决策修订记录 |


**下一步该做什么,看 §1 的"当前建议起点"标记。**

---



## 1. 分阶段清单



### 阶段 0 · 地基验证(不写代码)


| 任务                     | 产出                         | 状态                               |
| ---------------------- | -------------------------- | -------------------------------- |
| A1 手机备忘录连记 5 天饮食       | `planning/worknotes.md` 记录 | 需人工完成,AI 不能代做                    |
| A4 三个 prompt 测双边推理链稳定性 | 同上                         | 需人工完成                            |
| A2 徒手列 ≥10 条冲突场景       | 同上                         | **已被 §1.2 的 40 条规则表间接覆盖,可视为已达标** |


这一阶段是纯人工判断,不进入"文件/测试"体系,跳过不写。

---



### 阶段 1 · 判断力 + 冲突规则表


| 任务                       | 文件                           | 完成判据                                | 状态                                                    |
| ------------------------ | ---------------------------- | ----------------------------------- | ----------------------------------------------------- |
| 冲突规则表扩到 ≥30 条,每条带 source | `evals/conflict_rules.jsonl` | ≥30 条且 source 非空;三类关系(一致/互补/冲突)都有覆盖 | ✅ **已完成**(40 条,18 verified/22 needs_source)           |
| 建 repo + 第一个 commit      | `.git/`                      | `git log` 能看到至少一条记录                 | ✅ 仓库已存在(`https://github.com/jeyqc6/tcm_diet_expert.git`) |


---



### 阶段 2 · RAG + 检索层 ★


| 任务                           | 文件                                                                                | 测试/验证方式                                                                                                               | 状态                                                                                                                                                                                                                                                                                                                                  |
| ---------------------------- | --------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Step 1 最烂版 RAG               | `planning/step1-naive-rag/naive_rag.py`                                           | 手算 recall@5                                                                                                           | ✅ 已完成                                                                                                                                                                                                                                                                                                                               |
| Step 2 归因 + Step 3 改动对照表     | `docs/DECISIONS.md` D2/D3/D23                                                     | 归因表写进决策文档                                                                                                             | ✅ 已完成(体现在 D2/D3/D23 的论证里)                                                                                                                                                                                                                                                                                                           |
| 结构优先切分 + token 感知二次切分        | `planning/step1-naive-rag/ingest.py`                                              | 跑通全部真实知识源,产出 `knowledge/_processed/*.jsonl`                                                                           | ✅ 已完成                                                                                                                                                                                                                                                                                                                               |
| Embedding + 入库               | `db/embed_bge_m3.py`、`db/build_knowledge_base.py`                                 | `SELECT count(*) FROM knowledge_chunks` 对得上                                                                           | ✅ 已完成(5837 条,tcm 4447 + nutrition 1390)                                                                                                                                                                                                                                                                                             |
| 溯源(citation grounding)的确定性部分 | `backend/agents/citation.py`(新增,不在原骨架清单里)                                         | `tests/unit/agents/test_citation.py`(7 条全过)+ 真实检索结果做过端到端冒烟(幻觉引用能被抓出来)                                                 | ✅ **已完成**引用格式约定(`[source: chunk_id]`)、prompt 拼装、id 存在性校验;⏳ **未完成**"引用内容是否真的支持该结论"——这部分需要 LLM-as-judge/人工抽检,是 eval 范畴,不是本次能做的确定性代码;⏳ 还没接进真实 SubAgent 的 prompt(依赖阶段4的 adapter 层)                                                                                                                                                      |
| 混合检索(结构化预筛 + 向量排序) + 检索评分方法优化(dense+sparse 融合 + MQE) | `backend/mcp_server/tools/{_retrieval_common,retrieve_tcm,retrieve_nutrition}.py`、`db/{schema.sql,embed_bge_m3.py}`、`backend/agents/citation.py` | `tests/unit/mcp_server/test_retrieval_filters.py`(7 条)+ `tests/unit/mcp_server/test_retrieval_fusion.py`(RRF/MQE/同步异步桥接，纯函数)+ `tests/unit/agents/test_citation.py`(score 指引)+ 真实按体质代码过滤验证过 | ✅ **检索逻辑本身已完成并跑通真实数据**;⚠️ 发现并修了 `ingest.py` 的一个数据质量 bug(体质代码字段带 markdown 反引号,导致精确匹配永远失败),已修复 `ingest.py` 且手工修正了线上 9 行数据;⚠️ 例子里的 `source_status='verified'` 字段在 `knowledge_chunks` 表不存在(那是 `conflict_rules` 专属概念),`filters` 实际支持的是"任意 metadata 键值精确匹配",更通用但不含这个字段,细节见 `_retrieval_common.py` 模块文档;⏳ 还没挂进 MCP 协议层(`server.py`,阶段4任务4)。**2026-08-30 补充(检索评分方法优化,见 DECISIONS.md D35/ARCHITECTURE §2.6)**：`knowledge_chunks` 新增 `sparse_embedding sparsevec(250002)` 列(BGE-M3 词法权重，维度=实测 XLM-R tokenizer vocab_size)，`retrieve_tcm`/`retrieve_nutrition` 默认打开混合检索(dense+sparse，RRF 融合)+ MQE(LLM 查询改写，最多 2 个变体)；`RetrievedChunk.score` 语义调整为"融合后最高原始相似度"，`citation.py` 新增 `build_score_guidance_instruction()` 教模型怎么用这个字段（之前算出来没人用）；已对本机 5837 条真实 chunk 跑一次全量 `embed_bge_m3.py load` 回填 `sparse_embedding`。⚠️ **重跑 `EVALUATION.md` §7.7.3 官方评测，结果喜忧参半，不是单纯提升**：strict 60.0%→66.7%；但官方 Recall@5(组平均)76.7%→**73.3%，下降**——定位到稀疏通道把词面重合度高但内容更泛的段落排到了具体指导章节(gold chunk)前面。同一次跑分里 MQE 因为 dev 档免费模型限流大半没真正生效，"MQE 真正生效后综合效果"仍未知——已决定不为了这个数字继续消耗真实 prod 档 API 配额，如实标注为未解决的已知限制，候选缓解方向记在 `EVALUATION.md` §7.7.3 末尾。 |


> **agentic RAG 增强(MQE/HyDE)属于这里,但明确排后**——见 ARCHITECTURE.md §2.6(新增小节),不是本阶段的阻塞项,等 baseline recall 数字稳定之后再考虑要不要加。

---



### 阶段 3 · Eval 集冻结 + 早期 baseline


| 任务                                                      | 文件                         | 完成判据           | 状态                               |
| ------------------------------------------------------- | -------------------------- | -------------- | -------------------------------- |
| 写 `evals/dataset.jsonl` ≥40 条(E1 15/E2a 10/E2b 5/E3 10) | `evals/dataset.jsonl`      | 测试集冻结,写下冻结日期   | ✅ 2026-08-26 冻结,40 条             |
| 抽出 `smoke.jsonl` 15 条                                   | `evals/smoke.jsonl`        | 日常迭代跑它         | ✅ 15 条                           |
| 跑 B0(BM25+模板)/B1(单次LLM)/B3(通用助手)                        | `docs/EVALUATION.md` 第一组数字 | 三个 baseline 有数 | ✅ B0 53.3%(8/15)；B1/B3 阶段 8 双档见 `EVALUATION.md` §7 |


**⚠️ 这一阶段必须在阶段 4 写路由/SubAgent 之前完成**——D9"eval 先于实现",顺序反了会导致循环论证(roadmap 第七节第 5 条点名 AI 协作时最容易在这里翻车)。

---



### 阶段 4 · 骨架 + 五段管线 ★(核心编码阶段,顺序不能乱)

下表按 roadmap 4.2 的编号顺序排列,**编号即执行顺序**,后面每一项都依赖前面。


| #   | 任务                                         | 实现文件(占位已建)                                                                                                  | 设计依据                                | 测试文件(占位已建)                                                                | 完成判据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| --- | ------------------------------------------ | ----------------------------------------------------------------------------------------------------------- | ----------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | LLM adapter 层                              | `backend/llm/adapter.py`                                                                                    | ARCHITECTURE §7,DECISIONS D19       | `tests/unit/llm/test_adapter.py`(18 条全过,注入假 client/sleep,不打真实网络)          | ✅ **已完成**:超时分层(单次调用20s,靠 client timeout)、重试+指数退避+jitter(1s/2s/4s,429/5xx/超时可重试,400/401/内容策略拒绝不重试)、熔断器(连续5次失败打开,30s半开,打开时 prod→dev 降级)、双档切换(`MODEL_TIER` 环境变量)、`force_prod_tier` 参数(显式声明"这次调用必须用交付档,不受 `MODEL_TIER` 影响",熔断打开时仍会降级——真出故障时的兜底,不是主动省钱决策,两者不矛盾;D19 原本要求调和层永远传这个参数,2026-08-27 撤销,目前没有调用点使用,留作以后需要时用)。✅ 编排层 45s/90s 超时已接线(`backend/agents/timeouts.py`,2026-08-29)。⏳ 未完成:`naive_rag.py`/`run_baselines.py` 里内联的 LLM 调用还没有迁移过来用这一层(技术债,未处理)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 2   | Postgres schema 补全 + conflict_rules ingest | `db/load_conflict_rules.py`                                                                                 | ARCHITECTURE §1.2                   | 手工验证(无独立 pytest 文件,脚本自带 `--dry-run` 校验)                                   | ✅ **已完成并在真实 Postgres 上跑通**(2026-08-26):全量同步(UPSERT + 按 `rule_id != ALL(...)` 删除 JSONL 里已不存在的行,不是只增不减)。真实验证过四轮:①首次导入 40 条 ②原样重跑,`GROUP BY rule_id HAVING count(*)>1` 查出来是空的,无重复 ③从 JSONL 删掉最后一条重跑,表精确同步到 39 条,恢复后变回 40 条 ④抽查 `W01` 全部字段(`relation`/`confidence`/`applicable_constitutions`/`applicable_goals`/`source_status`)逐一比对 JSONL 原文,内容一致不只是数量对。`relation` 分布(conflict 15/complementary 8/aligned_negative 5/conditional_conflict 4/aligned 3/partial_conflict 2/nutrition_internal 2/tcm_internal 1)与 JSONL 统计完全吻合                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 3   | `query_diet_log` 工具                        | `backend/mcp_server/tools/query_diet_log.py`                                                                | ARCHITECTURE §2.2,D30               | `tests/unit/mcp_server/test_query_diet_log.py`(22 条全过)+ 真实 Postgres 端到端验证 | ✅ **已完成**:相对日期解析(今天/昨天/前天/本周/上周/最近N天/显式 YYYY-MM-DD)+ `by_ingredient`/`by_property`/`by_meal_type`/`raw` 四种聚合(`by_meal_type` 2026-08-26 经确认后补,`registry.py` 枚举与 ARCHITECTURE §2.2 同步更新了),真插入真实数据验证过。`by_nutrient` 明确 `NotImplementedError`。**时区待决问题已解决(D30)**:`user_profile` 新增 `city`/`timezone` 字段,`_get_tz()` 按 `user_profile.timezone` > `DIET_EXPERT_TZ` 环境变量 > 硬编码 `Asia/Shanghai` 三层兜底,任一层非法 IANA 时区名会跳过退到下一层。真实验证过:插入一条 `timezone='America/New_York'` 的画像,解析结果确实是纽约时区而不是环境变量配的上海;没有画像的用户正确退到环境变量。⚠️ 早前测试中意外撞见的美东/上海时区不一致现象(见本行历史)现在已经是"按用户画像各自解析"能正确处理的场景,不再是留白问题;`city` 字段本身还没有消费者(`query_weather` 未实现),但字段已经就位。首次引导流程(§11.2)已同步补上收集 city/确认 timezone 这一步                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| 4   | MCP server 骨架 + 权限分层                       | `backend/mcp_server/server.py` + `tools/*.py`                                                               | ARCHITECTURE §2                     | `tests/unit/mcp_server/test_tool_whitelist.py`                            | ✅ 越权调用在协议层被拒绝,不是应用层 if 判断——`DietExpertMcpServer` + 角色 session,11 条单测通过                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 5   | 中枢 agent + Agent Loop                      | `backend/agents/agent_loop.py`(原 `router.py` 已拆)                                                                     | ARCHITECTURE §3                     | `tests/unit/agents/test_agent_loop.py`(新增,7 条全过)                          | ✅ **已完成**:`run_agent_loop()` 实现 §3.2 步骤 1-7——终止条件是这一轮 `LLMResult.tool_calls` 是否为空,不是轮数计数;`max_tool_calls` 只是防失控循环的安全兜底(触顶抛 `AgentLoopResourceLimitError`,不是悄悄当正常结束处理),和"终止条件"是两条分开的路径。单工具/多工具并发调用/越权工具调用(协议层拒绝,错误喂回模型而非让 loop 崩溃)均有测试覆盖,用真实 `DietExpertMcpServer` + stub handler,不打真实网络。⚠️ 顺带发现并修复了一个前置缺口:`backend/llm/providers/{base,anthropic_provider,openai_compatible}.py` 此前完全不支持 tool_use(`stop_reason` 文档原话"工具调用等,本项目目前用不上"),没有这个 loop 就跑不起来——已补上 `tools`/`tool_calls` 的双向翻译(OpenAI 的 function-calling 包装 vs Anthropic 原生 tool_use/tool_result content block,含"同一 assistant 回合后的多个工具结果要合并进一条 user 消息"这条 Anthropic 硬性要求),`tests/unit/llm/test_providers.py` 新增 4 条覆盖两家 provider 的 tools 翻译/解析。⏳ 未完成:还没接 `classify_route` 的六条分支输出(该用哪个 system prompt / 要不要派发 SubAgent),那是把这个 loop 真正接进中枢 agent 业务流程的下一步,不在"loop 骨架"范围内                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| 6   | 六条分支路由                                     | `backend/agents/routing.py`                                                                                  | ARCHITECTURE §5.1,DECISIONS D12/D25 | `tests/integration/test_routing.py`                                       | 六条分支各喂 5 个 query,分类对;"记录回顾 vs 事实查询""候选评估 vs 完整推荐"边界不混淆                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 7   | 两个 SubAgent                                | `backend/agents/tcm_subagent.py`/`nutrition_subagent.py`                                                    | ARCHITECTURE §5.2 步骤3               | `tests/integration/test_subagent_loop.py`(8 条全过)                          | ✅ **已完成**:新增 `backend/agents/_subagent_common.py`(仿 `_retrieval_common.py` 的先例,两个 SubAgent 循环编排完全复用 `run_agent_loop()`,不重新实现循环)。领域隔离靠 §2.3 协议层白名单强制(越权工具在对应角色的 MCP session 里根本不存在),`run_subagent()` 打印"loop done · tools_called=[...]"日志作为可核查旁证——单测验证了 TCM 侧日志/messages 里不出现 `retrieve_nutrition`,反之亦然,以及"模型硬要调对方工具"时被协议层拒绝、不执行、不计入 `tools_called`(区分靠 `router.py` 新增的 tool 消息 `ok` 标志,不靠解析 JSON 内容猜)。资源限额按 §5.4 传 `max_tool_calls=15`(不是 router.py 中枢自己的 50),触顶抛 `AgentLoopResourceLimitError`。⚠️ **顺带实现了 §5.4"循环防护(连续3轮无新增信息)"**:`run_agent_loop()` 新增 `stall_round_limit` 参数(中枢不传,默认关闭;SubAgent 传 3),按"工具名+参数+结果"算内容指纹判断"这一轮有没有产生新信息",连续 3 轮没有就直接终止(不再多问模型一次去"总结"),这条只有 §5.4 的表格提到、行完成判据没点名,但属于"SubAgent 循环终止条件的一部分",按设计文档实现。D28"体质未知时 TCM SubAgent 降级为普适性建议"的措辞已接入 `build_tcm_system_prompt(constitution=...)`,但 `constitution` 目前是外部传入的参数,读 `user_profile` 这一步还没接(那是中枢编排层的职责,不在本任务范围)。`user_profile`/`query_diet_log` 聚合结果注入任务上下文这条也还没接,`task_input` 目前只是用户原始提问文本(D25 要求的那部分已满足)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 7.5 | SubAgent 循环状态提示                            | `backend/memory/status_prompt.py`                                                                           | ARCHITECTURE §4.5                   | `tests/unit/memory/test_status_prompt.py`(全过)                             | ✅ **已完成**:`build_status_message()` 纯代码拼接"[状态] 已用工具调用:{n}/15(资源限额,§5.4)· 已检索到的候选信息要点:{短列表}"，只扫 assistant 消息的 `tool_calls` 参数,不碰 role="tool" 的执行结果(不携带检索原文)。挂载点是 `run_agent_loop()` 新增的 `before_next_call` 可选 hook,`_subagent_common.py` 传入、中枢自己调用时不传,不侵入不需要这条状态提示的循环。状态消息 `role="user"` 而不是 `"system"`——因为 `anthropic_provider.py` 会把所有 `role="system"` 消息抽出来合并进顶层 `system` 参数,不管原来在对话历史哪个位置,"必须出现在最新位置"这条 D27 硬约束会被破坏,`role="user"` 才会被两家 provider 都按原位置保留                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| 8   | 调和层                                        | `backend/agents/reconciliation.py`                                                                          | ARCHITECTURE §5.2 步骤5,DECISIONS D14 | `tests/integration/test_reconciliation.py`(7 条全过)                         | ✅ **已完成**:D14"只收两侧结论,不接原始检索内容"这条边界靠**函数签名**强制,不是靠约定——`reconcile()` 只接受 `tcm_conclusion`/`nutrition_conclusion` 两个纯文本参数,类型层面就传不进 `SubAgentResult`(其 `.messages` 里是原始 tool_result,含检索到的 chunk 原文)。`reconcile_subagent_results()` 是给上游用的薄封装,只取 `.final_text`,并打日志记录"这一侧原本有多少条 messages 被丢弃、没有转发"(`test_reconcile_never_forwards_raw_chunk_content_via_subagent_results` 构造了一段"原始 chunk 标记文本"塞进 mock 的 `SubAgentResult.messages`,断言它没有出现在真正喂给 LLM 的 prompt 里,同时日志里能看到 `messages=3(discarded, not forwarded)`)。调和层本身**无工具**,不经过 `run_agent_loop`,是单轮 `complete()` 调用;跟随 `MODEL_TIER`,不强制 `force_prod_tier`(D19 原例外已于 2026-08-27 撤销,见决策修订记录);加载 `reconciliation_rubric` Skill(D22 确定性加载,不按分支条件);"候选评估分支走 D25 新增规则"体现为该 Skill 里的 harm-reduction 小节,按 pipeline 步骤确定性加载,不需要为候选评估单独分支。⏳ 未实现:D20 agent 行为点 #2"依据不足时回退请求 SubAgent 补充查询(建议上限1次)"——这条 retry 编排跨越调和层/SubAgent/中枢三方,中枢编排层本身还不存在,留给接线中枢编排时再做;"命中的 conflict_rules"目前是外部传入的 `matched_rules` 参数,规则匹配本身(按体质/目标从表里查出相关规则)未实现,不在本任务范围                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 9   | 核查 pass                                    | `backend/agents/verification.py`                                                                            | ARCHITECTURE §5.2 步骤6,DECISIONS D15 | `tests/integration/test_verification.py`(9 条全过)                           | 手工构造无 source_id 的建议,看它被移除而不是被补全                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 10  | 3 份基础 Skill + registry                     | `backend/skills/{reconciliation_rubric,verification_checklist,recipe_and_shopping_list}.md` + `registry.py` | ARCHITECTURE §6                     | `tests/unit/skills/test_registry.py`                                      | Skill 内容确实被拼入对应调用的 prompt,不是常驻 system prompt                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 11  | 流式输出 + FastAPI 最小闭环                        | `api/main.py`、`api/schemas.py`                                                                              | ARCHITECTURE §10,DECISIONS D26      | `tests/integration/test_api_chat_sse.py`(8 条全过)                           | ✅ **已完成,只做** `/api/chat` **一条路由**(`/api/profile`、`/api/onboarding/`* 依赖首次引导/CCMQ等未实现业务逻辑,明确不做,见 `api/schemas.py` 模块文档)。核查在第一条 token 前完成靠**代码结构**强制:`_verify_and_stream()` 里 `verify()` 的 `await` 排在任何 `yield "token"` 之前,不是约定。六条分支全覆盖:`log_write`→诚实返回 `not_implemented` guardrail(依赖的 `dish_decomposition.py`/`write_memory.py` 还是占位,不假装能写);`log_review`→直接查 `query_diet_log` 确定性格式化,不经过 LLM;`fact_query`/`single_domain`→按 `domain_hint` 单派发;`candidate_eval`/`full_recommend`→双派发+`reconcile_subagent_results`,`asyncio.gather(return_exceptions=True)` 单侧失败降级单边输出(ENGINEERING §2 坑一)。⚠️ **诚实说明"流式"的真实程度**:`adapter.complete()` 是一次性拿完整响应,不是逐 token 生成;这里把算好的整段文本切块依次 `yield` 多个 `token` 事件,做对的是 SSE 传输层机制,不是模型级流式——真正的流式需要 adapter/provider 两层都加 streaming 支持,是另一块更大的工作,不在本任务范围。**顺带修了一个真实 bug**:`router.py` 的 `_execute_tool_call` 用 `json.dumps(default=str)` 序列化 dataclass 类型的工具结果(比如 `retrieve_tcm` 返回的 `list[RetrievedChunk]`),会变成 Python repr 字符串塞进 JSON 数组,不是结构化对象——下游没法从里面精确取出 `source_id`;改成 dataclass 感知的 `_json_default`(`dataclasses.asdict`)。**还给** `RouteDecision` **加了** `domain_hint` **字段**(`fact_query`/`single_domain` 分支该查 `retrieve_tcm` 还是 `retrieve_nutrition`,原来的六分支分类完全没回答这个问题,API 层没法派发)。真实端到端验证过三轮:①`log_review` 真查 Postgres 空表/有数据两种情况 ②`log_write` 诚实返回未实现 ③`fact_query` 先用本地 Ollama(`qwen3:0.6b`)跑,模型直接跳过检索工具凭"记忆"回答、核查因缺 `source_id` 正确拦截(D15"只拒绝不改写"在真实弱模型场景下工作正常的活例子)；换成 Anthropic Haiku 重跑,模型正确调用 `retrieve_tcm`、拿到真实 chunk(`tcm_002541`)、正确引用、通过核查、`source` 事件确实排在 `token` 事件之前 |
| 12  | 前端最小闭环                                     | `frontend/`(`create-next-app` 建的最小 App Router + TS 项目,不带 Tailwind)                                          | PRD §7                              | 无自动化测试(纯前端手工/构建验证)                                                        | ✅ **已完成,单页聊天界面**:`src/app/page.tsx` 手写 SSE 解析(浏览器 `fetch`+`ReadableStream`，`EventSource` 只支持 GET 用不了,和 `api/main.py` 的 `event:.../data:...` 格式对应),渲染 `token`(增量拼接)/`source`(引用标签)/`guardrail`(拦截提示)三类事件。后端加了 `CORSMiddleware`(前后端不同源)。真实验证:`npm run build` 通过;真启动前后端两个 dev server,curl 确认页面渲染、CORS 头正确；用 Node 原生 `fetch`/`ReadableStream`(和浏览器同一套 Web API)模拟浏览器的 SSE 解析逻辑,对真实后端跑通一整轮"红枣是什么性味"对话,拼出的文本和引用(`tcm_002541`/`tcm_002228`)都正确。⚠️ **诚实说明局限**:①没有在真实浏览器里点击验证过——这个环境没有浏览器自动化工具,如实说明不是漏做；②"溯源可展开"是简化版,只显示 `source_id` 标签,不能点开看 chunk 原文(需要后端加一个按 chunk_id 查原文的端点,§10.1 当前路由清单没有,是下一步)；③没有会话历史持久化(`GET /api/sessions/{id}/messages` 未实现,刷新页面历史就没了)。详见 `frontend/README.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 13  | `docker compose up`                        | 新增 `docker-compose.yml`(不在当前骨架里,阶段 4 收尾时再建)                                                                 | ENGINEERING §9                      | —                                                                         | ✅ **已完成**(2026-08-26):完成判据"删本地卷重来一遍,90s 内可用"实测 `docker compose down -v && docker compose up -d` 后 **14s** `/healthz` 返回 200(镜像已 build 过;首次 `docker compose build` 装 torch 和首次拉 BGE-M3 不计入这 90s)。启动顺序按 §9:Postgres → schema init → `seed`(40 条 `conflict_rules`,秒级,走启动路径)→ API → 前端。`knowledge_chunks` ingest **不**在启动路径上(§9 硬约束):空库 healthz 照样 200,聊天会因无 `source_id` 被核查拦截——这是设计如此,不是漏做。灌检索数据用 `docker compose --profile ingest up ingest`(可跳过);本机验证时容器 CPU 嵌入约 0.8 条/秒、5837 条是小时级,所以改从宿主机已有的 `diet_expert` 库 `pg_dump` 进容器(14s 拷完 4447 tcm + 1390 nutrition)。Postgres 发布端口 **5433**(避开本机已占用的 5432,否则 host 侧脚本会连错库)。`/healthz` 在 compose 里设 `HEALTHZ_CHECK_DB=1` 才查 DB+pgvector+HNSW 索引,单测不设这个变量、仍是进程存活。真实验证:灌库后 `"红枣是什么性味？"` 通过核查,`source` 事件 `tcm_002541`/`tcm_002228` 排在 `token` 之前,`accepted=1 rejected=0`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |




**⭐ 与上表并行,不能推后**(ENGINEERING 各章节):超时分层、重试+jitter、写路径幂等键、`trace_id` 贯穿、ingest 幂等+字段校验、GIN 索引、`CACHE_DISABLED` 开关——这些不是独立任务,是上表每一项实现时就要带上的属性,不是写完主逻辑再回来补。

---



### 阶段 5 · 安全层


| 任务                | 文件                                                        | 测试文件                                           | 完成判据                    |
| ----------------- | --------------------------------------------------------- | ---------------------------------------------- | ----------------------- |
| 输入防护 + 输出拦截       | `backend/guardrails/input_filters.py`/`output_filters.py` | `tests/unit/guardrails/test_input_filters.py`(24 条全过)/`test_output_filters.py`(9 条全过) | ✅ **已完成**:5 个手工构造的恶意输入(中文指令注入/英文 prompt injection/夹带 System: 前缀/"以上都不算"变体/DAN 式 jailbreak)全部被 `filter_input()` 拦下并剥离，见 `test_five_handcrafted_malicious_inputs_are_all_caught`。`strip_instructions()` 只删指令片段、保留食物实体原文(不是整段丢弃)；`truncate_input()`(>2000字)、`detect_medical_intent()`(疾病/用药意图)同样有边界值测试。输出侧：诊断性表述、"多多益善"式表述检测已实现。**已接线**：`_stream_chat()` 在路由判断前跑输入防护(总览图①)；`verification.py` 新增诊断性表述确定性检查(编号8) |
| ED 防护四条           | `backend/guardrails/ed_protection.py`                     | `tests/unit/guardrails/test_ed_protection.py`(全过) | ✅ **已完成**：四条规则(数值化表述/极端限制/索要数值目标/自述低摄入或体重焦虑)均为确定性正则，覆盖 THREAT_MODEL.md E3 穷举的换皮样本(千分位数字、中文数字大写、BMI、体脂率、"两位数的千卡"绕数字表述、双语)。命中后返回 `docs/prompts/disclaimers.md` §9/§10 的审阅过模板，不临场生成；四条按"体重焦虑>索要目标>数值表述>极端限制"排优先级(`primary`)。**已接线**：输出侧(规则1/2)替换了 `verification.py` 里原本的窄正则；输入侧(规则2/3/4)接进 `_stream_chat()`，命中直接返回模板、不产生任何 LLM 调用 |
| 过敏原硬阻断            | `backend/guardrails/output_filters.py`(与 §4.2 菜品拆解共用集合比对逻辑，函数签名对齐) | `tests/unit/guardrails/test_allergen_block.py`(29 条全过，逐条穷举 `HIDDEN_ALLERGEN_SOURCES` 种子表每一条映射) | ✅ **已完成，且已扩展为三层防御(2026-08-27)**：①**生成前**——`allergens` 参数接进 `run_tcm_subagent()`/`run_nutrition_subagent()`，`_subagent_common.py` 的 `build_allergen_avoidance_instruction()` 把用户过敏原 + 反查出的隐藏来源词写进 system prompt，明确要求"优先调整食材/调味来避开，而不是整道菜不推荐"；②**命中重试**(仅 `full_recommend`/`candidate_eval` 双派发路径)——`api/main.py` `_stream_dual_dispatch` 检测到调和层输出命中过敏原时，带着具体反馈(`reconcile()` 新增 `avoid_note` 参数)重调一次调和层，上限 1 次；③**输出侧硬阻断**(原有)——直接命中(过敏原类别字面词)+ 隐藏来源命中("蚝油→甲壳类"等 19 条种子映射)两条路径，`verification.py` 确定性检查(编号4)命中即移除、不重生成，作为前两层没生效时的最终兜底。`UserProfileContext.allergens` → `_verify_and_stream()`/`_run_verification()` → `verify(user_allergens=...)` 全链路真实生效，`test_allergen_hit_blocks_final_answer_end_to_end`/`test_allergen_hit_triggers_reconciliation_retry_then_succeeds`/`test_allergen_retry_passes_through_after_cap` 端到端验证。双派发调和重试过后 `verify(skip_allergen_check=True)` 不再跑第 4 条；「今天避开：」类清单不算命中。⏳ 未覆盖：`log_write` 写库前只警告不阻止(设计如此,见 THREAT_MODEL.md E2)；`fact_query`/`single_domain` 单派发路径没有调和层，不支持命中重试；`HIDDEN_ALLERGEN_SOURCES` 是种子表不是穷举数据库；核查 pass 把整轮回复当一个条目，命中后是整段拒绝不是按菜/按条目拒绝 |
| `THREAT_MODEL.md` | `docs/THREAT_MODEL.md` | — | ✅ **已完成**(2026-08-26 初版;guardrails 落地后同日更新):PRD §15「≥5 个具体逃逸场景」写成 8 条可复现攻击(E1 饮食记录间接注入 / E2 过敏原别名与隐藏成分 / E3 ED 四条换皮 / E4 假引用洗白 / E5 SubAgent 越权工具 / E6 工具循环 / E7 知识库 chunk 投毒打调和层 / E8 诊断用药当医嘱)。每条标设计控制、**现在拦不拦得住**、残留逃逸;不把「模型应该拒绝」当控制。对照 OWASP LLM Top 10 补了本项目暴露面。**guardrails 三文件实现 + 接线后已回填最新状态**:E2/E3/E8 从"占位/真空"更新为"部分或大部分拦得住",残留部分(写路径 `dish_decomposition.py`、`critical_fact_scanner.py`、委婉问法漏检)如实标注,不假装比实际做到的更多 |


---



### 阶段 6 · 可观测 + 测试与 CI


| 任务                    | 文件                                      | 完成判据                                                          |
| --------------------- | --------------------------------------- | ------------------------------------------------------------- |
| Langfuse 全链路接入        | `backend/observability/` + `backend/llm/adapter.py` 埋点 | ✅ **已完成**(2026-08-26)：一条请求一条 trace（`X-Trace-Id` 头 + SSE `done.trace_id`，32 hex，可直接当 Langfuse trace id）。span 树：`chat` → `router` / `subagent.*` / `reconcile` / `verify` / `llm.complete`(generation，含 tokens/`cost_est`/熔断降级) / `tool.*`。没配 `LANGFUSE_PUBLIC_KEY`+`SECRET_KEY` 时是 no-op；默认脱敏（PRD 健康数据不上报明文），`LANGFUSE_CAPTURE_IO=1` 才传原文。单测 `tests/unit/observability/test_tracing.py` + adapter/provider/SSE 贯穿用例。不把 Langfuse 整套栈塞进 `docker-compose.yml`（会打破 §9 90s 冷启动） |
| Record/Replay fixture | `backend/llm/providers/replay.py`(`ReplayProvider`/`replay_provider_for`)+ `tests/fixtures/llm_replay/` | ✅ **已完成(2026-08-27)**:`ReplayProvider` 实现 `base.py` 的 Provider 协议,通过 `complete()` 已有的 `provider=` 注入点使用,业务代码(router/subagent/reconciliation)零改动。指纹 = `(model, messages, tools, 其余 kwargs)` 的规范化 JSON 摘要(`sort_keys=True`,和字段顺序无关);replay 模式查不到指纹直接报错(`ReplayFixtureMissing`,经 `complete()` 包装成 `NonRetryableError`,原始异常留在 `__cause__`)——这就是"prompt 被意外改动"的检测器,不是缺陷。`replay_provider_for(caller)` 靠 `LLM_REPLAY_MODE` 环境变量切换:默认(未设置)`replay`,CI 不需要任何 LLM API key;`record`(+ 真实凭据)才会构造真实 provider 打网络。18 条单测(`test_replay_provider.py`,注入假 inner,不打真实网络)+ 2 条集成测试(`test_llm_replay.py`)。真实验证过:`test_llm_replay.py` 真实调用过一次 Anthropic Haiku 录制了 `demo_router_call__*.json`,之后在**完全清空 `ANTHROPIC_API_KEY`/`OPENROUTER_API_KEY`/`LLM_PROVIDER_DEV`/`LLM_MODEL_DEV`/`MODEL_TIER` 的环境**下重跑仍然全过(离线、零成本这条判据的直接证明);同一份测试也验证了"消息内容变了→指纹变了→查不到旧 fixture"这条漂移检测行为 |
| 故障注入 fixture          | `tests/fixtures/fault_injection/`(`FaultInjectingProvider` + 预制故障类/`MALFORMED_JSON_RESPONSES`) | ✅ **已完成(2026-08-28)**:实现 `base.py` 的 Provider 协议，`classify_error` 逐条照抄两家真实 provider(`anthropic_provider.py`/`openai_compatible.py`)的判定逻辑，不是简化版。`RateLimitFault`(429)/`ServerErrorFault`(5xx)/`TimeoutFault`(超时)三类映射到 retryable，`AuthFault`(401)映射到 non_retryable，`ContentFilterFault`覆盖"内容策略拒绝不重试"这条(靠 `stop_reason` 不靠 `classify_error`)。`MALFORMED_JSON_RESPONSES` 提供三个真实变体(代码块包裹/JSON后跟解释文字/完全不是JSON)。17 条单测(`test_fault_injection.py`)：既验证这个 fixture 本身的判定逻辑，也把三个 JSON 变体接到 `backend/agents/routing.py` 真实的 `_parse_route_llm_json()` 上验证——顺带确认了 D32 那条"prose-after-JSON 是已知残留缺口"的记录已经过时:`_strip_json_fences()` 现在会在去掉代码块围栏后定位第一个括号平衡的 `{...}` 片段，两种变体都能正确解析,只有"完全不是JSON"才返回 `None`(未回填 D32 原记录,如实记在这里)。也验证了熔断:连续失败达到阈值(5)后 `circuit.is_open`,`force_prod_tier=True` 的调用会被自动降级到 dev 档。 |
| CI pipeline           | `.github/workflows/ci.yml` + `ruff.toml` + `evals/run_baselines.py` 新增 `--check-launch-threshold` | ✅ **已完成(2026-08-28)**:三个 job 串行(`lint`→`test`→`smoke-eval`，`needs:` 链式依赖)。**lint**：新增 `ruff.toml`，只选 `E`(pycodestyle)+`F`(pyflakes)、忽略 `E501`(这份代码库的中文长注释风格用不上行宽限制)——这份代码库此前从未跑过 lint，先用"真实正确性问题"这个小得多的规则集把 CI 跑绿，不是一次性开满 ruff 默认的一大堆风格规则去先报几百条历史问题；修了跑出来的 4 个真实问题(2 处未使用的 import、1 处未使用的变量、1 处刻意的 sys.path 修补后置 import 加 `# noqa: E402` 精确豁免)。**test**：跑 `pytest tests/`，显式设 `LLM_REPLAY_MODE=replay`(即便这就是默认值)——不需要任何 secrets，因为所有 LLM 调用要么是 DI 注入的假 `complete()`，要么走 record/replay fixture 离线回放。**smoke-eval**：`run_baselines.py` 新增 `--check-launch-threshold`，M1(检索 recall@5)跌破 `docs/EVALUATION.md` §4 Launch 阈值(70%)就 `sys.exit(1)`，本地用真实 15 条 `smoke.jsonl` 验证过(当前 recall@5=40%，正确触发失败退出)。⚠️ **诚实说明两处限制**：①`knowledge/_processed/{tcm_chunks,nutrition_chunks}.jsonl` 是 `.gitignore` 排除的派生数据，全新 checkout 默认没有，workflow 检测文件不存在时跳过并打印原因，不是每次都能跑——需要专门决定"要不要把派生数据放进制品缓存"这类基础设施问题，不在这次范围内；②M3(中医方向一致率)/M5(冲突调和正确率)两项 Launch 阈值目前完全没有自动化 eval 实现(`docs/EVALUATION.md` 原文"阶段 8 在此文件追加...")，`--check-launch-threshold` 因此只能检查 M1，不是文档原句"任一指标"字面意义上的全部三项——这不是这次的疏漏，是如实反映这两项的自动化跑分本来就还没做。 |


---



### 阶段 7 · 分层压缩 ★(技术制高点) — **Partial，不是 Done**

**剩余(2026-08-29 对照代码)**：

| 缺口 | 现状 |
|---|---|
| 独立 3 日菜谱组装 | Skill 已在完整推荐+菜谱问法时加载；没有单独步骤 7 管线 |
| 双侧失败 naive RAG | 仍是 guardrail，不另建管线 |
| 空检索静态兜底 | 无；核查拒绝 |
| B1/B3 / B2 | 阶段 8 |


| 任务                                    | 文件                                                               | 测试文件                                                                          | 完成判据                                         |
| ------------------------------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------------------- | -------------------------------------------- |
| 关键事实落库前置扫描                            | `backend/memory/critical_fact_scanner.py`                        | `tests/unit/memory/test_critical_fact_scanner.py`                             | ⚠️ **Partial**(扫描+立即 UPSERT 已完成,2026-08-28)。**缺人在环**：命中后立刻 `write_memory(critical)` 并用于本轮生成，违反 PRD §10.2。历史完成记录：纯扫描模块(`scan_allergen_mentions`/`scan_supplement_mentions`/`scan_critical_facts`)，零 I/O 依赖，`ALLERGEN_CATEGORY_KEYWORDS`(9 类,过敏声明句式:"对X过敏"/"X过敏"/"过敏原是X")+ `SUPPLEMENT_KEYWORDS`(常见非处方补剂,"我在吃/服用X")逐条穷举,各自的否定语境词表(单独两份,和 output_filters.py 的否定词表不共用——否定"过敏声明"和否定"菜里含某成分"是两类不同语境)防止"没有对虾过敏"这类表述被误判成命中；`scan_critical_facts()` 返回相对现有 `UserProfileContext` 的**增量**(已记录过的不重复上报)；`merge_into_profile()` 把增量与现有画像合并成 write_memory 的 payload(UPSERT 是整列覆盖语义,必须传完整合并后的列表,不能只传新增的)+ 更新后的 `UserProfileContext`，供**同一轮**请求剩余流程使用(核心场景：一句话里"顺带"声明的过敏原，这一轮 SubAgent 生成建议时就该已经避开，不用等下一轮)。`api/main.py` `_stream_chat_inner` 接线：路由判断之前、onboarding 检查之后跑一次，命中就同步走 MCP client 调 `write_memory(critical)`，吐 `critical_fact_recorded` guardrail 事件，并把合并后的 `profile` 变量传给后续 `dispatch_branch`/`stream_multi_task`。**类别命名对齐** `backend/guardrails/output_filters.py` 的 `HIDDEN_ALLERGEN_SOURCES`(甲壳类/鱼类/乳制品/麸质/芝麻/花生)，新增三类(坚果/大豆/蛋类)补全 `knowledge/_raw/allergen/condiment-allergens-cn.jsonl` 里的"中美并集九类"；扫描到的是类别不是原文用词，否则新写入的类别不会被 `check_allergens()` 的隐藏来源比对层认出来。**顺带修的两处缺口**：`write_memory.py` 的 `_CRITICAL_COLUMNS` 此前漏了 `supplements` 列(会被当成未知字段直接拒绝写入)；`UserProfileContext`/`_row_to_context` 此前完全不读 `supplements` 字段(列存在但读不出来)。184 条单测全过(`test_critical_fact_scanner.py` 175 条,逐条穷举每个类别/关键词 + 否定语境穷举，覆盖率 100%；`test_user_context.py`/`test_write_memory.py` 各新增数条覆盖 `supplements` 字段)+ 2 条集成测试(`test_api_chat_sse.py`：命中时验证落库 payload、guardrail 事件、TCM SubAgent 系统提示词里确实带上新过敏原的避让指令三件事同时成立；未命中时验证正常聊天不触发 write_memory)。⏳ **仍未做**：`UserProfileContext.supplements` 目前只写不读——SubAgent/调和层/核查 pass 尚未消费这个字段(该怎么影响生成的建议是单独的设计问题)；`dose` 恒为空；过敏声明只认明确句式，不含糊到"提一句食材就算"。|
| 分层压缩(优先级表 + 结构化归档摘要 + 两级触发) + 接进真实请求生命周期 | `backend/memory/{compression,session_store}.py` + `backend/llm/model_capabilities.py` + `api/main.py`/`backend/agents/dispatch.py` 接线 | `tests/unit/memory/test_compression.py`(37条)+ `tests/unit/llm/test_model_capabilities.py` + `tests/integration/test_session_store.py`(12条，真实Postgres)+ `tests/unit/agents/test_dispatch.py`+`tests/unit/api/test_turn_accumulator.py`+`test_api_chat_sse.py`读侧接线测试 | ⚠️ **Partial**(会话归档已接；Level 1 未接线)。历史：算法本身(`compression.py`)：压缩优先级表(`compress_retrieved_chunks()`，未引用的chunk直接丢、被引用的只留`source_id`)、D27固定模板结构化摘要(`build_archived_summary()`，纯确定性组装不调用LLM)、两级触发(`should_compress_retrieval()`/`should_archive_tier1()`/`select_turns_to_archive()`保底留`keep_recent`轮原文/`is_session_idle()`/`drop_oldest_until_within_budget()`从Tier3不从Tier1丢)全部是不碰数据库/FastAPI的纯函数；`model_capabilities.py`落实"不同LLM上下文窗口不同"——`context_window_for_model()`前缀匹配表+ `LLM_CONTEXT_WINDOW_OVERRIDE`环境变量覆盖，压缩阈值按实际窗口缩放PRD §12.3假设的预算(只收紧不放大)。**真正接进`/api/chat`请求生命周期**(新增`session_store.py`)：`db/schema.sql` `messages`表补5列(`branch`/`conclusion`/`cited_source_ids`/`rejected_suggestions`/`triggered_guardrails`)；`record_turn()`(步骤9写入+触发归档)/`maybe_fold_idle_session()`(新消息到达这个天然时机检查空闲)/`load_session_history()`(步骤2读取+同步兜底)均实现并对真实本地Postgres验证过。`api/main.py`新增`get_session_history_loader`/`get_turn_recorder`/`get_idle_session_folder`三个DI注入点(同`get_complete_fn`模式，测试换空操作假函数，不写真实库、不同测试间不因复用同一硬编码session_id互相污染)；`_TurnAccumulator`观察`dispatch_branch()`已产出的SSE事件流重建`TurnRecord`，不改`dispatch.py`任何函数签名；写入用`asyncio.create_task(asyncio.to_thread(...))`触发不`await`(没有Celery，这是FastAPI原生等价物)。读侧(`dispatch.py`新增`_compose_task_input()`)只接给`fact_query`/`single_domain`/`candidate_eval`/`full_recommend`四个会派发SubAgent的分支。构造了20轮对话(第2轮披露过敏原)的场景测试证明结构化摘要在Tier流转后仍可被`find_summaries_mentioning()`查到；真实验证：本地真实uvicorn+Postgres，`fact_query`/`other`/`single_domain`三种分支各发一条真实消息，`messages`表都正确出现对应行(含一条被过敏原guardrail拦截、`conclusion`为空但`triggered_guardrails`/`rejected_suggestions`正确记录的失败路径)。⏳ **仍未接线(如实记录)**：SubAgent内Level1触发(检索结果压缩)——`should_compress_retrieval()`/`compress_retrieved_chunks()`两个纯函数已实现，但"未被引用的chunk"这个判断依赖SubAgent的`final_text`，而触发检查按设计发生在循环**进行中**(每次`tool_result`追加后，此时最终答案还不存在)，这个时序矛盾写`compression.py`时就发现了，接线需要专门设计，这次没有强行接上去糊弄；`user_profile`(Tier0)/`critical_fact_scanner.py`是"过敏原信息端到端永久生效"真正的机制，本行只负责会话历史+检索结果这条压缩，两者已独立存在但是各自的职责。 |
| 菜品拆解三级查找 + 晋升逻辑                       | `backend/memory/dish_decomposition.py`/`dish_alias_promotion.py`、`backend/mcp_server/tools/write_memory.py`(daily_log 分支)、`api/main.py`(log_write 分支) | `tests/unit/memory/test_dish_decomposition.py`(20条)/`test_dish_alias_promotion.py`(4条)、`tests/unit/mcp_server/test_write_memory.py`(11条)、`tests/unit/api/test_log_write_helpers.py`(16条)、`tests/integration/test_api_chat_sse.py` 相关用例 | ✅ **全部完成**(2026-08-27)。三级查找：全局表(`knowledge/food/dish-decomposition.jsonl`，不是 Postgres 表——现 217 条，§1.2"待建表"清单本来就没有它)按最长菜名优先做子串扫描 → 命中已晋升的 `user_dish_aliases`(整句归一化后精确匹配)→ LLM 兜底(`confidence=low`)。晋升：`ON CONFLICT ... WHERE promoted_at IS NULL` 保证已晋升的行不再计数，真实验证过恰好第3次命中触发 `promoted_at`、晋升后第4次调用 hit_count 不再增长、且三级查找里第二级确实能查到刚晋升的这行。`write_memory(daily_log)` 用 `INSERT ... ON CONFLICT (idempotency_key) DO NOTHING` 做幂等，真实验证过同一分钟内重复提交同一条消息不产生第二行。`api/main.py` 的 log_write 分支：过敏原即时警示(警示不阻断——记录的是已发生的事，硬阻断该用在生成新建议的路径上)、meal_type 关键词+时段兜底推断、logged_at 相对日期解析(今天/昨天/前天)。**顺带发现并修的两个真实 bug**：①最初任何非空残留文本都会退回LLM，导致"帮我记录一下，中午吃了麻婆豆腐"这种已经命中已知菜名的输入仍会为纯叙述性填充词多打一次LLM——改成"已有命中且残留文本不含加/和/还有等连接词就不再退回LLM"；②真实调用 Anthropic Haiku 发现，即便 system prompt 明确要求"只输出严格JSON"，模型仍会把JSON包在```json代码块里，直接`json.loads`报错——照抄 `router.py`/`verification.py` 里已有的 `_strip_json_fences` 同款处理(这三处目前各自复制一份，不是共用函数，和既有代码风格一致)。meal_type 关键词最初要求"早上吃"连在一起才命中，"早上刚喝了..."这类漏检，已改成单独时间词也命中。真实端到端验证：已知菜(不打LLM)/未知食物(LLM兜底)/幂等去重/别名晋升/过敏原警示均在真实本地 Postgres + 真实 Anthropic Haiku 上跑通。⏳ **未做**：§4.2 步骤4"人在环确认"简化为"先幂等写入、再展示结果"，不是原文"先展示、等确认/修改"的往返(`/api/chat` 单发SSE，没有编辑已记录条目的端点)。 |
| `dish_ingredient_map` 数据扩充到 100-200 条 | `knowledge/food/dish-decomposition.jsonl`                        | `tests/unit/memory/test_dish_decomposition.py`(全局表命中/最长优先/单字「饭」不入表) | ✅ **已完成**(2026-08-28)。`knowledge/food/dish-decomposition.jsonl` **217 条**(菜名唯一)，略超原目标 100-200 的上限，种子表可以接受。在家常菜基础上补了能单独吃的主食/食材(白米饭/米饭/白饭/糙米饭/粗粮饭/杂粮饭/南瓜/红薯/玉米/豆腐等)，让「中午吃了南瓜」「番茄炒蛋加一碗白米饭」走全局表而不是 LLM 兜底。⚠️ **故意不收录单字「饭」**——子串扫描会把「晚饭/午饭/吃饭」误判成一碗米饭；更长菜名优先的既有规则仍然成立(「黄焖鸡米饭」不会被拆成「米饭」)。⏳ **未跑**：原完成判据里的「E3 记忆子集 eval 测覆盖率」还没有单独跑一版数字，留给阶段 8 全量 eval；这次验收的是条数达标 + 查表行为单测，不是覆盖率分数。 |
| 首次引导 + CCMQ 计分 | `backend/onboarding/{flow,ccmq_scoring}.py`、`api/main.py`、`backend/mcp_server/tools/write_memory.py` | `tests/unit/onboarding/{test_ccmq_scoring,test_flow}.py`(46 条)+ `tests/unit/mcp_server/test_write_memory.py`(6 条)+ `tests/integration/test_api_onboarding.py`(8 条)，共 60 条全过 | ✅ **全部完成**(2026-08-26)。CCMQ 计分见上一版本记录(转化分公式/平和质单独规则/体质夹杂/边界值，均已实现)。**新增：§11.2 对话式引导步骤(`flow.py`)+ `POST /api/onboarding/{start,answer}` + `GET/PATCH /api/profile`(`api/main.py`)+ `write_memory` 的 `category="critical"` 分支(`write_memory.py`，供引导结果落库)**。两处文档本身没回答、这次做出的具体选择(已写进模块文档)：①多轮 `/api/onboarding/answer` 之间的状态是客户端原样带回的不透明 `state` dict，不新建数据库表；②过敏原/偏好的自由文本抽取用比 §4.2 简单得多的规则(分隔符切词+否定词判断)，不等 `dish_decomposition.py`。CCMQ 问卷环节全程确定性代码，不调用 LLM。真实端到端验证：对本地真实 Postgres 走完整条"过敏原→偏好→城市→时区确认(D30 显式确认，不静默套用建议值)→体质自述/CCMQ 问卷分批作答→确认"流程，`write_memory(critical)` 真实写入 `user_profile`，`GET /api/profile` 读回来字段完全一致。⚠️ **顺带发现并修复一个真实 bug**：`.env` 的 `DIET_EXPERT_PG_DSN` 一直是个连不上的占位值(`postgresql://user:pass@localhost/...`，角色 `user` 在本机 Postgres 不存在)，本机(非 docker)运行时这条 DSN 从来没真的连上过库，`fetch_user_profile()` 的失败静默降级恰好把这个问题掩盖了(表现为"查不到画像"而不是报错)——已修正为真实可用的本机连接串。⏳ **仍未做**：`write_memory` 的 `category="daily_log"` 分支(依赖 `dish_decomposition.py`)；`/api/chat` 不会因画像不完整自动切换成引导对话，`GET /api/profile` 的 `onboarding_recommended` 字段只是把 §11.1 触发条件暴露给前端，由前端决定何时调用 `/api/onboarding/start`(§10.1 没有把"自动触发"设计进 `/api/chat`)。 |


---



### 阶段 8 · 重跑 eval + B2 ablation ★(简历数字诞生)


| 任务                                    | 完成判据                       |
| ------------------------------------- | -------------------------- |
| B2(单agent+双库同上下文)实现并对比                | ✅ **有结论(2026-08-29 keyword rubric + 2026-08-30 LLM-judge 复核)**:两轮跑分方向一致、幅度不同。keyword rubric 版:B2 在 E2a+E2b(15 条)pass_rate(40.0% vs 26.7%)、延迟(203.2s vs 414.3s)、LLM 调用次数(33 vs 78)三项均优于当前架构。抽查发现该 rubric 对同义词不宽容，于是加做了 `evals/run_llm_judge.py`(五维度语义评分，对同一批回答重新打分，不重跑 SubAgent)——**内容质量在语义评分下两边几乎打平**(baseline 7.93/8 vs B2 8.00/8，14/15 条可解析)，13.3 个百分点的原始差距主要是字面用词造成的。**延迟/调用次数的差距不受影响，依然是 B2 用不到一半成本打平质量**。已按约定在 `DECISIONS.md` D1 下追加两版修订记录，`docs/EVALUATION.md` §4.1 补了两版数字。**"回退"这一步(把生产链路 `dispatch.py`/`reconciliation.py` 真的换成单 agent)诚实标注为未执行**——n=15+免费开发档模型(生成和裁判都是同一个模型)的结果强度，以及 D1 理由二(可分别评估的工程可维护性)完全没被这两轮 ablation 覆盖，这两点决定了要不要动生产代码需要人决定范围，不是分数过了阈值就自动改生产架构。新增 `backend/agents/single_agent_baseline.py`(B2 实现，`CallerRole.SINGLE_AGENT_B2`，只用于 ablation 不接生产路径)+ `evals/run_b2_ablation.py` + `evals/run_llm_judge.py`(均可复现)。 |
| 全量 eval 交付档 + 开发档各跑一次                 | ✅ **2026-08-29 keyword + 2026-08-30 LLM-judge 复评**(见 `docs/EVALUATION.md` §7 / §7.6)。B0 M1 仍 **53.3%**。keyword：开发档 M3 53.3%/M5 26.7%；交付档 gpt-4o M3 40.0%/M5 6.7%。judge（免费模型、同义转述算对）：开发档 B1 M3 **93.3%**/M5 **100%**；交付档 B1 M3 **66.7% Launch**/M5 **80% Stretch**。开发档 Stretch 不能当简历数字（裁判=生成模型）。原始:`evals/results/phase8_2026-08-29.json`、`evals/results/phase8_judge_2026-08-30.json`。 |
| 分切片报告(特禀质/补剂交互/症状类/weight_management) | ✅ keyword 见 §7.3；**LLM-judge 见 §7.6.2**。B1 judge 特禀质 semantic 80%/60%；过敏原附加项开发档 **100%**（2026-08-30 人工改判 E3-01）、交付档 60% 仍未达标。补剂 85.7%/85.7%；症状 100%/60%；weight 100%/80%。 |
| 三档阈值对照                                | ✅ keyword 下 M1/M3/M5 均未达标。**judge 复评后**：交付档 B1 M3 **Launch**(66.7%)、M5 Stretch(80%)。**生产向量官方 M1 = labeled Recall@5 76.7%**（组平均，domain 池，**Launch**；两组都召回的 all_hit 53.3%，短板是节气表 `tcm_000029`–`tcm_000033`；BM25 B0 地板仍是 53.3%）。见 `EVALUATION.md` §7.7。 |


产出:`docs/EVALUATION.md`(阶段 3 初版已创建;阶段 8 补 B2 ablation、双档数字、分切片与三档阈值对照)。

---



### 阶段 9 · 收口


| 任务              | 完成判据                       |
| --------------- | -------------------------- |
| 英文 README + 架构图 | ✅ **已完成(2026-08-30)**:新写 `README.md`(英文，GitHub 首页默认渲染这个)，旧的中文项目索引改名 `README.zh.md`，`README.md` 顶部加一行互链。架构图用 Mermaid flowchart 直接嵌进 `README.md`（GitHub 原生渲染，不是外部图片链接），内容对照 `ARCHITECTURE.md` §0 总览图；用 `@mermaid-js/mermaid-cli` 本地渲染过一次确认语法没问题、关键节点文字确实出现在渲染结果里，不是没验证过的手写 mermaid。内容包含：问题陈述、差异化说明(双 agent 隔离上下文+调和层+只拒绝不改写的核查+role-scoped MCP 工具)、按 `BUILD_PLAN.md`/`EVALUATION.md` 如实转译的阶段状态表、B2 ablation 的双轮结论摘要(含"两轮怎么变化、为什么"的说明，不只是甩一个分数)、Docker/本地两条起步路径、真实测试过的部署方式(Docker Compose 自建)与诚实标注**未搭建**的部分(Vercel+Render+Neon 云部署)。 |
| 2 分钟英文录屏        | ⏳ **未做**——需要真人录屏操作，不是能在这个环境里生成的产出物 |
| 部署或录屏代替         | ⚠️ **只有"一键自建"这一半是真的**：`docker compose up` 已验证能跑（见 RUN.md）。Vercel+Render+Neon 云部署**没有**搭建、没有配置文件、没有跑通过——`README.md` 里如实说明这一点，不冒充已完成 |


---



## 2. 当前建议起点

阶段 4 主链路已接通。**下一个该做的不是阶段 8**，而是把阶段 4–7 的审计剩余项做完(见 README「当前欠账」)：

1. P0/P1 审计项已按代码落地（天气、HITL pending、fallback 测试、CI fixture、Level 1 FIFO、D20 #2 一轮重试、菜谱 Skill 最小接线、补剂/偏好进 prompt、前端 SSE、追问/引导进 Postgres、`conflict_gaps.jsonl`）。阶段 7 仍标 Partial。
2. 不要把阶段 8（B2 ablation / 全量 eval / Launch 70%）当成已经完成。
3. 45s/90s 超时**已经接线**，不要当未做项。
4. 双侧失败仍是 guardrail，没有 naive RAG——不要在文档里假装有。
